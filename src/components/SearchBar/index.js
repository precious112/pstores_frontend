import React from 'react';

import { Wrapper,InputBox,InputButton } from './SearchBar.styles';

const SearchBar = () => (
<Wrapper>
    <InputBox/>
    <InputButton>Search</InputButton>
</Wrapper>

);

export default SearchBar;